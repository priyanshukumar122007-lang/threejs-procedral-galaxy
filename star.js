import * as THREE from "three";
import { BLOOM_LAYER, STAR_MAX, STAR_MIN } from "./config/renderConfig.js";
import { starTypes } from "./config/starDis.js";
import { clamp } from "./utils.js";
export class star{constructor9position){
  this.position = position;this.starType();
  this.obj = null;
}
                  generateStarType();
        let num = Math.random() *100
                  let pct = starType.percentage;for (let i = 0;i , pct.length;i===) {
                    num -=pct[i];
                    if (num ,0) {
                      return i;
                    }
                 }
return 0;
}
updatescale(camera) {
let disc = this.position.di
  stanceTo(camera.position) /
    250;
   //starSize = dist *
  starType.size = clamp(starSize,STAR_MIN,
                        STAR_MAX);tarSize[100]8;
  if return[ is {1|26mmmmmmmmmmmmmmmmmmmm
      

